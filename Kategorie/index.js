//AUDI
// Add an event listener to the select element
document.getElementById('marke').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'a3':
      window.open('../cars/audi/exp/index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    case 'a4':
      window.open('../cars/audi/exp/index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    case 'a5':
      window.open('../cars/audi/exp/index.html', '_blank'); // Open 'page4.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('type').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'kombi':
      window.open('../cars/audi/exp/index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    case 'suv':
      window.open('../cars/audi/exp/index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    case 'limousine':
      window.open('../cars/audi/exp/index.html', '_blank'); // Open 'page4.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('kategorie').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'neu-eingetroffen':
      window.open('../cars/audi/exp/index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    case 'top-cars':
      window.open('../cars/audi/exp/index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('antrieb').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'allrad':
      window.open('../cars/audi/exp/index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    case 'vorderrandartrieb':
      window.open('../cars/audi/exp/index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('antrieb').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'saab':
      window.open('../cars/audi/exp/index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    case 'opel':
      window.open('../cars/audi/exp/index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    case 'audi':
      window.open('../cars/audi/exp/index.html', '_blank'); // Open 'page4.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('getriebe').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'automat':
      window.open('../cars/audi/exp/index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    case 'automatic':
      window.open('../cars/audi/exp/index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('treibstoff').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'benzin':
      window.open('../cars/audi/exp/index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    case 'diesel':
      window.open('../cars/audi/exp/index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    case 'elektro':
      window.open('../cars/audi/exp/index.html', '_blank'); // Open 'page4.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
//FINISHED

//BMW
document.getElementById('marke-1').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case '320':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    case '530':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    case '740':
      window.open('../index.html', '_blank'); // Open 'page4.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('type-1').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'cabriolet':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    case 'coupe':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    case 'limousine':
      window.open('../index.html', '_blank'); // Open 'page4.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('kategorie-1').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'neu-eingetroffen':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    case 'top-cars':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    case 'sport-cars':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('antrieb-1').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'allrad':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    case 'hinterrandantrieb':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('getriebe-1').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'manual':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    case 'automatic':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('treibstoff-1').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'benzin':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    case 'disel':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    case 'elektro':
      window.open('../index.html', '_blank'); // Open 'page4.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
//FINNISHED

//FERRARI
document.getElementById('marke-4').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case '458':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    case '488':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    case 'F40':
      window.open('../index.html', '_blank'); // Open 'page4.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('type-4').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'cabriolet':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    case 'coupe':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('kategorie-4').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'sport-cars':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('antrieb-4').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'hinterrandantrieb':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('getriebe-4').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'automatic':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('treibstoff-4').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'benzin':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
}); 
//FINNISHED

//HYUNDAI
document.getElementById('marke-6').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'i30':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    case 'tucson':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('type-6').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'limousine':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    case 'suv':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('kategorie-6').addEventListener('change', function() {
 
});
document.getElementById('antrieb-6').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'vorderrandartrieb':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('getriebe-6').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'automatic':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('treibstoff-6').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'benzin':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
      case 'hybrid':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
}); 
//FINNISHED

//LAND ROVER
document.getElementById('marke-10').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'discovery':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    case 'range-rover':
      window.open('index.ht', '_blank'); // Open 'index.ht' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('type-10').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {

    case 'suv':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('kategorie-10').addEventListener('change', function() {
 
});
document.getElementById('antrieb-10').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'allrad':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('getriebe-10').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'automat':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('treibstoff-10').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
   
    case 'benzin':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
 //FINISHED

//Maserati
document.getElementById('marke-11').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'mc-20':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('type-11').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {

    case 'coupe':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('kategorie-11').addEventListener('change', function() {

  
 
});
document.getElementById('antrieb-11').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'hinterradantrieb':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('getriebe-11').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'automat':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('treibstoff-11').addEventListener('change', function() {

  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
   
    case 'benzin':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
//FINISHED

//Mercedez-Benz
document.getElementById('marke-12').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'amg-gt':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
      case 'c200':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
      case 'c350':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
      case 'c63-amg':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
      case 'e300':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('type-12').addEventListener('change', function() {

  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'bus':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;case 'cabrioulete':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;case 'coupe':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;case 'kombi':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    case 'limousine':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('kategorie-12').addEventListener('change', function() {
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'sports-cars':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('antrieb-12').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'allrad':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    case 'hinterradantrieb':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
      case 'vorderrandartrieb':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('getriebe-12').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'automat':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
      case 'automatic':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('treibstoff-12').addEventListener('change', function() {

  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'benzin':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
      case 'disel':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
//FINISHED

//Porsche
document.getElementById('marke-13').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case '911':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
      case 'coyenne':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('type-13').addEventListener('change', function() {

  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
      case 'cabrioulet':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
      case 'coupe':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
      case 'suv':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('kategorie-13').addEventListener('change', function() {
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'sports-car':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('antrieb-13').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'allrad':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    case 'hinterradantrieb':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('getriebe-13').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'automat':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('treibstoff-13').addEventListener('change', function() {

  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'benzin':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
//FINISHED

//Renault
document.getElementById('marke-14').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case '911':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
      case 'coyenne':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('type-14').addEventListener('change', function() {

  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
      case 'cabrioulet':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
      case 'coupe':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
      case 'suv':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('kategorie-14').addEventListener('change', function() {
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'sports-car':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('antrieb-14').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'allrad':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    case 'hinterradantrieb':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('getriebe-14').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'automat':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('treibstoff-14').addEventListener('change', function() {

  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'benzin':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
//FINISHED

//Renault
document.getElementById('marke-15').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case '911':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
      case 'coyenne':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('type-15').addEventListener('change', function() {

  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
      case 'cabrioulet':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
      case 'coupe':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
      case 'suv':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('kategorie-15').addEventListener('change', function() {
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'sports-car':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('antrieb-15').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'allrad':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    case 'hinterradantrieb':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('getriebe-15').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'automat':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('treibstoff-15').addEventListener('change', function() {


  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'benzin':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
//FINISHED

//Skoda
document.getElementById('marke-16').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case '911':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
      case 'coyenne':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('type-16').addEventListener('change', function() {

  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
      case 'cabrioulet':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
      case 'coupe':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
      case 'suv':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('kategorie-16').addEventListener('change', function() {
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'sports-car':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('antrieb-16').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'allrad':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    case 'hinterradantrieb':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('getriebe-16').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'automat':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('treibstoff-16').addEventListener('change', function() {

  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'benzin':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
//FINISHED

//Tesla
document.getElementById('marke-17').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case '911':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
      case 'coyenne':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('type-17').addEventListener('change', function() {

  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
      case 'cabrioulet':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
      case 'coupe':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
      case 'suv':
      window.open('../index.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('kategorie-17').addEventListener('change', function() {
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'sports-car':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('antrieb-17').addEventListener('change', function() {
  // Get the selected option valu7
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'allrad':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    case 'hinterradantrieb':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('getriebe-17').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'automat':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('treibstoff-17').addEventListener('change', function() {

  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'benzin':
      window.open('../index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
//FINISHED

//VW
document.getElementById('marke-18').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'passat':
      window.open('../cars/vw/model/passat.html', '_blank'); // Open '../index.html' in a new tab
      break;
      case 'touran':
      window.open('../cars/vw/model/touran.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('type-18').addEventListener('change', function() {

  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
      case 'kompaktvan/minivan':
      window.open('../cars/vw/model/touran.html', '_blank'); // Open 'index.ht' in a new tab
      break;
      case 'kombi':
      window.open('../cars/vw/model/passat.html', '_blank'); // Open 'index.ht' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('kategorie-18').addEventListener('change', function() {
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'top-car':
      window.open('../cars/vw/index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('antrieb-18').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'allrad':
      window.open('../cars/vw/index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    case 'vorderradantrieb':
      window.open('../cars/vw/index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('getriebe-18').addEventListener('change', function() {
  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'automat':
      window.open('../cars/vw/index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});
document.getElementById('treibstoff-18').addEventListener('change', function() {

  // Get the selected option value
  var selectedOption = this.value;
  // Redirect to the corresponding page based on the selected option
  switch(selectedOption) {
    case 'diesel':
      window.open('../cars/vw/index.html', '_blank'); // Open '../index.html' in a new tab
      break;
    default:
      // Handle other cases if needed
      break;
  }
});